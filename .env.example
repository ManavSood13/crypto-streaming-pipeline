# Copy to .env and adjust. All values optional; defaults target a local PostgreSQL.

PGDATABASE=crypto_pipeline
PGUSER=
PGHOST=localhost
PGPORT=5432
# PGPASSWORD=

# Logging
LOG_LEVEL=INFO
LOG_MAX_BYTES=5242880
LOG_BACKUP_COUNT=3
